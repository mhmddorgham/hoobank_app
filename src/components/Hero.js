import React from 'react';
import styles from '../style';

const Hero = () => {
  return (
    <section id='home' className={`flex md:flex-row flex-col ${styles.paddingY}`}>
      <div className={`flex-1 ${styles.pa}`}>

      </div>
    </section>
  )
}

export default Hero